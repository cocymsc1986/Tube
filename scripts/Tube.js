export default function showBalance() {

  return balance;
}

export default function updateBalance(balance, value) {
  
  return balance;
}

export default function enterStation(balance, zone) {
  
}

export default function leaveStation(balance, zone) {
  
}